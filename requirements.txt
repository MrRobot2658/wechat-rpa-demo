# Android RPA 核心依赖
uiautomator2>=3.0.0        # Android UI 自动化框架
adbutils>=2.0.0             # ADB 工具库（uiautomator2 依赖）

# UI 元素检查工具（开发调试用）
# uiautodev                 # 推荐的 Web UI 查看器（pip install uiautodev）
# weditor                   # 备选的 Web UI 查看器

# 可选依赖
Pillow>=9.0.0               # 截图处理
